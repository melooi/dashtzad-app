/* =============================================================================
 * Dashtzad — Checkout UX helper
 * -----------------------------------------------------------------------------
 * این فایل هیچ منطقِ تجاری ندارد: ثبتِ سفارش، محاسبهٔ مبلغ، روش‌های ارسال،
 * کدِ تخفیف و پرداخت همگی توسطِ خودِ WooCommerce (checkout.js هسته + هوک‌ها)
 * مدیریت می‌شوند. اینجا فقط یک کمکِ ظاهریِ بی‌خطر است.
 *
 * هیچ مبلغ/آدرس/درگاهِ hardcode، هیچ‌گونه onclick/oninput inline.
 * ========================================================================== */
(function () {
	'use strict';

	var form = document.querySelector('form.woocommerce-checkout');
	if (!form) {
		return;
	}

	// نشانهٔ حضورِ صفحهٔ تسویه برای استایل‌های اختیاریِ دشت‌زاد.
	document.body.classList.add('dz-has-checkout');
}());
